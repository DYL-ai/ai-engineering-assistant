interface:
  display_name: "AI Engineering Assistant"
  short_description: "Turn complex engineering work into a controlled, verifiable, reusable AI workflow"
  default_prompt: "Use $ai-engineering-assistant to organize this long-running engineering task into a contract, Agent roles, evidence acceptance, and knowledge feedback loop."
policy:
  allow_implicit_invocation: true
